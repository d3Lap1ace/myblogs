---
title: claude code 使用技巧
date: "2026-04-20"
category: code
readTime: 8 min
---

## TL;DR 

| 分类            | 命令 / 操作                          | 功能说明                    |
| ------------- | -------------------------------- | ----------------------- |
| **会话管理**      | `claude -c`                      | 继续上次会话                  |
|               | `claude -r`                      | 交互式选择历史会话               |
|               | `claude --resume <id>`           | 精确恢复指定会话                |
|               | `/clear`                         | 清空上下文，开始新任务             |
|               | `/compact [focus on X]`          | 压缩上下文，可指定保留内容           |
|               | `/branch`                        | 创建对话分支，两条路径均保留          |
|               | `/rename` / `/color`             | 命名和标记颜色，多会话不混淆          |
| **快捷操作**      | `Esc`                            | 立即停止 Claude             |
|               | `Esc+Esc` / `/rewind`            | 回滚到检查点                  |
|               | `Ctrl+S`                         | 暂存输入草稿                  |
|               | `Ctrl+B`                         | 后台运行长任务                 |
|               | `Ctrl+G`                         | 外部编辑器打开输入框              |
|               | `Ctrl+V`                         | 粘贴剪贴板图片                 |
|               | `Shift+Tab`                      | 切换 Auto / Plan 模式       |
|               | `!<cmd>`                         | 直接执行 Bash 命令            |
| **模式切换**      | `/vim`                           | 开启 Vim 键位绑定             |
|               | `/focus`                         | 隐藏过程，只看结果               |
|               | `/effort low\|high\|xhigh`       | 调整 Opus 思考深度            |
|               | `/model <name>`                  | 切换模型（opus/sonnet）       |
| **上下文**       | `@file/path`                     | 精确引用文件                  |
|               | `/btw <问题>`                      | 悬浮侧问，不影响主上下文            |
|               | HANDOFF.md                       | 手动交接文档，跨会话继续任务          |
| **配置与权限**     | `/permissions`                   | 白名单信任常用命令               |
|               | `/fewer-permission-prompts`      | 自动扫描并推荐权限白名单            |
|               | `/config`                        | 设置输出风格                  |
|               | `/statusline`                    | 自定义底部状态栏                |
| **Hooks**     | PostToolUse                      | 编辑后自动格式化                |
|               | PreToolUse                       | 拦截危险命令（rm -rf 等）        |
|               | Stop                             | 任务完成触发通知/提示音            |
|               | SessionStart                     | 每次会话注入动态上下文             |
| **并行与 Agent** | `claude --worktree <name>`       | 创建隔离工作区并行开发             |
|               | `CLAUDE_ENABLE_TEAMS=1`          | 开启多 Agent 团队模式          |
|               | `/loop <interval> <task>`        | 定期轮询后台任务                |
| **工具与扩展**     | `.claude/skills/<name>/SKILL.md` | 自定义 Skill，`/name` 触发    |
|               | `.claude/commands/<name>.md`     | 自定义轻量 Slash Command     |
|               | `claude --add-dir <path>`        | 同时操作多个目录/仓库             |
|               | `claude --print -p "<prompt>"`   | Headless/SDK 模式，输出 JSON |
|               | `git diff \| claude -p "..."`    | Pipe 输入，非交互调用           |
| **诊断与费用**     | `/doctor`                        | 环境诊断，排障第一步              |
|               | `/cost`                          | 查看当前会话费用                |
| **环境变量**      | `CLAUDE_MODEL`                   | 设置默认模型                  |
|               | `DISABLE_AUTO_COMPACT=1`         | 禁用自动压缩                  |
|               | `CLAUDE_CODE_NEW_INIT=1`         | 启用新版 `/init` 向导         |

---

## 基础设置与快捷操作

**自定义状态栏**

底部显示模型、目录、Git 分支、未提交文件数、Token 进度条和最后一条消息。支持 10 种颜色主题，脚本见 [`scripts/context-bar.sh`](https://github.com/ykdojo/claude-code-tips/blob/main/scripts/context-bar.sh)。

**`/statusline` 两行布局提示词**

直接粘贴以下提示词即可配置两行状态栏：

```
/statusline 分两行显示，第一行模型显示名称、ctx left、当前 git 分支。第二行：5 小时和 7 天rate_limit，用bar+百分比+重制时间，参考：
Sonnet 4.6 | ctx 10% | branch: main
5h [███░░] 63% ~4h8m | 7d [██░░░] 40% ~4d15h
```

**`-c`、`-r` 和 `--resume`**

- `-c`：继续上次会话
- `-r`：交互式选择最近会话
- `--resume <session-id>`：精确恢复特定会话，session id 可在 `~/.claude/projects/` 目录下找到

**输入框导航快捷键**

- `Ctrl+A/E`：行首/行尾
- `Option+Left/Right`：按词跳转
- `Ctrl+W/U/K`：删词/删至行首/删至行尾
- `Ctrl+G`：在外部编辑器中打开输入框（适合粘贴长文本，比直接粘贴快）
- `Ctrl+V`（Mac）：粘贴剪贴板图片

**设置 `cc` 别名**

在 `~/.zshrc` 中添加 `alias cc='claude --dangerously-skip-permissions'`，跳过所有权限提示（需了解风险后使用）。

**Auto 模式（`--dangerously-skip-permissions` 的安全替代）**

权限请求由模型分类器自动判断是否安全，安全就直接放行，不用再盯着批准。你可以同时开多个 Claude 并行干活，一个在跑就切到下一个。CLI 里 Shift-Tab 进入 Auto 模式，Desktop 和 VSCode 在下拉菜单选择。Max、Teams、Enterprise 用户可用（目前灰度开启中，部分用户可能还看不到）。

**`!` 前缀直接运行 Bash 命令**

输入 `!git status` 或 `!npm test` 立即执行，输出会进入上下文供 Claude 分析。

**Esc 停止，Esc+Esc 回滚**

- `Esc`：立即停止 Claude，不丢失上下文
- `Esc+Esc`（或 `/rewind`）：打开检查点菜单，支持恢复代码、对话或两者
- 注意：Bash 命令执行（如数据库迁移）不在检查点范围内

**Ctrl+S 暂存提示词草稿**

写到一半想先问个问题？Ctrl+S 暂存草稿，快速提问后自动恢复。

**Ctrl+B 后台运行长任务**

把测试、构建等耗时操作放到后台，Claude 继续工作，完成后通知你。

**`/permissions` 白名单常用命令**

停止对 `npm run lint` 重复点击确认，白名单信任命令。

**`/fewer-permission-prompts` 自动推荐白名单**

内置技能，扫描你的历史会话，找出那些安全但反复触发权限弹窗的 bash 和 MCP 命令，然后推荐一批命令加入你的权限白名单。比手动配 `/permissions` 更省心，适合不用 Auto 模式的人。

**设置输出风格**

`/config` 选择 Explanatory（详细步骤）、Concise（简洁）或 Technical（技术精确）风格，或自定义。

**禁用 Co-authored-by 署名**

在 `~/.claude/settings.json` 中添加：

```json
{
  "attribution": {
    "commit": "",
    "pr": ""
  }
}
```

默认情况下 Claude Code 会在 commit 和 PR 中添加 co-author 信息，设置为空字符串即可禁用。ref: [https://code.claude.com/docs/en/settings#attribution-settings](https://code.claude.com/docs/en/settings#attribution-settings)

**`--model` 指定模型**

`claude --model opus` 启动时指定，或会话中 `/model sonnet` 切换。重型任务用 Opus，简单改动用 Sonnet 省钱。

**关键环境变量**

- `CLAUDE_MODEL`：设置默认模型
- `ANTHROPIC_API_KEY`：使用自己的 API Key
- `DISABLE_AUTO_COMPACT=1`：禁用自动压缩，配合 HANDOFF.md 手动管理上下文

**`--add-dir` 多目录工作**

```bash
claude --add-dir ../backend --add-dir ../shared
```

同时操作多个仓库，前后端联调、monorepo 子包协作时很有用。

---

## 提示词与上下文管理

**不相关任务之间用 `/clear`**

清空上下文比在混乱的长会话中挣扎更有效。

**`/btw` 快速侧问**

弹出悬浮层提问，不影响主上下文。实用场景：Claude 长时间没动静时，用 `/btw 是不是卡住了` 确认状态，不会打断当前任务。

**`@` 直接引用文件**

`@src/auth/middleware.ts` 精确指向文件，省去 Claude 自己搜索的代价。

**用模糊提示探索陌生代码**

"这个文件你会改进什么？" 让 Claude 主动发现问题，适合熟悉新代码库。

**HANDOFF.md 交接文档**

关闭自动压缩，手动让 Claude 写 HANDOFF.md（目标、已试什么、什么有效/无效、下一步），新会话只需提供文件路径即可继续。比 `/compact` 更可控。

**搜索对话历史**

对话存于 `~/.claude/projects/`，每个会话是 `.jsonl` 文件。可用 grep 搜索，或直接问 Claude "帮我找今天关于 X 的对话"。

**half-clone 减少上下文**

`half-clone-conversation.sh` 只保留对话后半段，比 /branch（两条路径都存活）更适合"续跑但减负"场景。可配合 Hook 在上下文超 85% 时自动触发。ref: [https://github.com/ykdojo/claude-code-tips/blob/main/skills/half-clone/SKILL.md](https://github.com/ykdojo/claude-code-tips/blob/main/skills/half-clone/SKILL.md)

**Auto Memory 自动记忆**

Claude 自动维护 `~/.claude/projects/<project>/memory/MEMORY.md`，记录构建命令、调试洞察、项目约定等。比手写 HANDOFF.md 更自动，适合长期项目积累。

**拖拽/粘贴图片直接分析**

把截图拖进终端或 `Ctrl+V` 粘贴剪贴板图片，Claude 可以直接看图。适合分析 UI Bug、报错截图、设计稿还原等场景。

**Auto-compact 自动压缩**

上下文接近窗口限制时自动触发压缩。可以用 `/compact focus on the API changes` 指定保留重点，也可以设置 `DISABLE_AUTO_COMPACT=1` 完全关闭，改用 HANDOFF.md 手动管理。

---

## CLAUDE.md 配置文件

**Claude 犯错后更新 CLAUDE.md**

说"更新 CLAUDE.md 让这个问题不再发生"，Claude 会自己写规则，下次会话自动遵守。

**`@imports` 保持 CLAUDE.md 精简**

用 `@docs/git-instructions.md` 引用外部文档，Claude 按需读取，不占主文件空间。

**CLAUDE.md 是建议，Hooks 是要求**

CLAUDE.md 遵守率约 80%，Hooks 是 100% 确定性执行。格式化、lint、安全检查等必须执行的操作用 Hooks。

**自动化的自动化**

发现反复重复的操作就自动化它：CLAUDE.md 记录、skills 封装、脚本化——把元效率做到极致。

---

##  Hooks 自动化

**PostToolUse Hook 自动格式化**

每次 Claude 编辑文件后自动运行 Prettier（配置在 `.claude/settings.json`）。

**PreToolUse Hook 阻止危险命令**

在 Bash 工具前拦截 `rm -rf`、`drop table`、`truncate` 等破坏性命令。

**Notification Hook 桌面通知**

Claude 需要用户注意时触发（如等待权限确认、任务空闲），可配置发送系统通知或声音。

**SessionStart Hook 动态注入上下文**

每次会话启动时执行，可运行脚本自动注入最近提交、环境状态、当前任务等信息，跨压缩保留关键上下文。配置在 `.claude/settings.json` 的 `hooks.SessionStart` 下。

**UserPromptSubmit Hook 预处理提示词**

用户提交提示词前触发，可自动附加上下文、替换关键词或拦截敏感操作。

**Stop Hook 任务完成播放提示音**

```json
"command": "/usr/bin/afplay /System/Library/Sounds/Glass.aiff"
```

让 Claude 完成时发出声音，你可以去做别的事。

---

## 并行与多 Agent

**`/effort` 配置思考深度**

Opus 4.7 用自适应思考（adaptive thinking）替代了思考预算。用 `/effort` 调整：低 effort 更快更省 token，高 effort 更智能。可选 low/medium/high/xhigh/max，其中 max 仅当前会话生效，其他级别跨会话保持。日常推荐 xhigh，最难的任务用 max。此外，在提示词中加入 `ultrathink` 仍可将单轮 effort 设为 high，触发更深层推理。

**不确定方案时使用 Plan Mode**

多文件变更、陌生代码、架构决策用 Plan Mode，小任务直接执行。Shift+Tab 切换模式。

**`--worktree` 并行独立分支**

`claude --worktree feature-auth` 创建隔离工作区，同时运行 2-3 个 worktree 并行开发不同功能。

**子 Agent 保持主上下文干净**

"用子 Agent 调查支付流程的错误处理"——让子 Agent 深挖代码，只返回摘要，主会话不被消耗。

**Agent 团队**

需先设置环境变量 `CLAUDE_ENABLE_TEAMS=1` 开启此功能。创建 3-5 个 Agent 团队并行重构模块。避免多个 Agent 修改同一文件。

**一个 Claude 写，另一个 Claude 审**

实现会话写代码，审查会话以高级工程师视角 review，两者上下文独立，效果更好。

**Headless / SDK 模式**

```bash
claude --print --output-format json -p "分析这段代码的复杂度"
```

把 Claude Code 当 SDK 用，在 Node/Python 脚本中调用，输出结构化 JSON。适合批量处理、CI/CD 集成。

---

## 工具与插件

**安装语言 LSP 插件**

TypeScript/Python/Rust/Go 等语言服务器插件让 Claude 在每次编辑后自动看到类型错误，是单个最高价值插件。

**使用 `gh` CLI 及教 Claude 任何 CLI**

`gh` 处理 PR/Issue，比 MCP Server 更省上下文。对于 Claude 不熟悉的工具，告诉它"用 `--help` 学习后使用"。

**手机远程控制 Claude**

`claude remote-control` 启动后，在手机浏览器或 App 上连接本地会话，发送消息、审批操作。

**扩展上下文窗口到 100 万 Token**

Sonnet 4.6 和 Opus 4.6 均支持 1M 上下文，`/model opus[1m]` 切换。

**Gemini CLI 访问被屏蔽网站**

Claude WebFetch 无法访问 Reddit 等，创建 skill 让 Claude 通过 tmux 调用 Gemini CLI 作为备用。

**从终端导出输出内容**

`/copy`：复制最后一次回复为 Markdown。

---

## 自定义扩展（Skills / Commands / MCP）

**自定义 Skills**

在 `.claude/skills/` 下创建目录，放入 `SKILL.md` 描述文件，即可封装复杂工作流。比如发推、分析仓库、写 PR Review 都可以做成 Skill，用 `/skill-name` 触发。Skill 可以包含脚本、配置和 prompt 模板，是 Claude Code 最强大的扩展机制。

**自定义 Slash Commands**

在 `.claude/commands/` 下创建 `.md` 文件即可注册命令。文件内容就是 prompt 模板，支持 `$ARGUMENTS` 占位符：

```markdown
<!-- .claude/commands/review.md -->
Review the following code for bugs, security issues, and performance problems: $ARGUMENTS
```

然后输入 `/review @src/auth.ts` 即可触发。比 Skills 更轻量，适合高频操作。

**MCP Server 集成**

通过 MCP（Model Context Protocol）连接外部服务——数据库、API、文档系统等。在 `.claude/settings.json` 的 `mcpServers` 中配置：

```json
{
  "mcpServers": {
    "my-db": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://..."]
    }
  }
}
```

适合需要查询生产数据库或内部 API 的场景。

**Pipe 输入 + `-p` 一次性命令**

```bash
git diff | claude -p "review this diff"
cat error.log | claude -p "分析这个报错"
echo "SELECT * FROM users LIMIT 5" | claude -p "优化这条 SQL"
```

非交互模式，输出到 stdout，适合集成到脚本和管道中。加 `--output-format json` 可输出结构化结果。

---

## 会话管理

**多标签页瀑布式管理**

新任务开新标签页，从左到右依次检查。同时处理 3-4 个任务为宜。最左标签保留固定工具（如语音转写）。

**压缩时指定保留内容**

`/compact focus on the API changes and the list of modified files` 告诉 Claude 压缩时保留什么。

**`/loop` 定期检查**

`/loop 5m check if the deploy succeeded` 设置后台轮询任务。注意：任务 3 天后自动过期，长期监控需改用 cron 或 GitHub Actions。

**语音输入（`/voice`）**

按住 Space 语音输入，口述提示词通常比打字更详细、背景更丰富。

**`/sandbox` 安全隔离模式**

macOS 用 Seatbelt、Linux 用 bubblewrap 进行 OS 级隔离，限制文件写入范围和网络请求。

**Focus 模式**

隐藏所有中间过程（命令执行、文件编辑），只展示最终结果。当你信任模型能跑对命令、做对编辑时，只看最终产出就够了。`/focus` 切换开关。

**`/branch` 保留两条路径**

创建对话分支尝试风险重构，原对话完整保留，与 rewind 的区别是两条路径都存活。

**命名和颜色标记会话**

`/rename auth-refactor`，`/color red`，多会话并行时避免输错终端。

---

## 其他

**`/doctor` 环境诊断**

环境出问题时跑 `/doctor`，自动检查权限、模型连接、配置文件、LSP 状态等。排障第一步，先跑 `/doctor` 再找原因。

**`/cost` 查看会话费用**

随时了解当前会话花了多少钱。对控制成本、评估不同模型的性价比有帮助。

**`/vim` 模式**

开启 Vim 键位绑定，`/vim` 切换。Vim 用户的体验大幅提升。

**自定义加载动画文字（彩蛋）**

把加载时的 spinner 词替换成你喜欢的内容，比如 "假装在思考中…"。

**Claude Code Cheat Sheet**

一页纸速查所有快捷键、Slash Commands、配置、MCP、Skills 等，打印贴桌上。网页版：https://news.ycombinator.com/item?id=47495527

**定时任务卡 5 小时重置节点**

Claude Code 有个 5 小时重置机制，可以通过云端定时任务卡重置节点，让每个工作时段都从满血状态开始。

设置三步：

1. 打开 [https://claude.ai/code/scheduled/new](https://claude.ai/code/scheduled/new)
2. 选 Custom，输入 `5 4,9,14,23 * * *`（注：不是 `5 7,12,17,22 * * *`，因为要考虑时区）
3. 点 Create

每天 7:05、12:05、17:05、22:05 自动触发一次对话，重置 5 小时倒计时。对应上午、下午、傍晚、深夜四个时段，每段都能用满 token 额度。任务跑在云端，本地不用开电脑，比 crontab 优雅。